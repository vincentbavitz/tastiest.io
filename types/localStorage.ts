export enum LocalStorageItem {
  HAS_ACCEPTED_COOKIES = 'HAS_ACCEPTED_COOKIES',

  /**
   * Sync the restaurants the user has followed while being
   * offline when they return back online.
   */
  SYNC_FOLLOWING = 'SYNC_FOLLOWING',
}
